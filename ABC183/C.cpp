#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  ll N,K;
  cin >> N >> K;
  vector<vector<ll>> tMat(N);
  vector<ll> ans();

  rep(i,N) {
    rep(j,N) {
      cin >> tMat.at(i).at(j);
    }
  }


}