#include <bits/stdc++.h>
#include <atcoder/dsu>
using namespace std;
using namespace atcoder;

int main() {
  int N, K;
  cin >> N >> K;
  vector<int> A(N)
  for (int i = 0; i < N;i++) {
    cin >> A[i]
  }

    for (int i = 0; i < N;i++) {
    cout << A[i]
  }
}